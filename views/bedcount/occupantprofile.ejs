<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/stylesheets/bootstrap.min.css" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="/javascripts/jquery-1.11.1.min.js"></script>
    <script src="/javascripts/bootstrap.min.js"></script>
    <script src="/javascripts/bootbox.min.js"></script>

    <title>Bedcount</title>
  </head>
  <body>

    <h1><%= occupant.name %></h1>
    <h4><%= occupant.age %> years old</h4>
    <h3>Current Location:
      <% if (occupant.daysLeft > 0) %>
        <%= occupant.currentLoc.shelterName + " (" + occupant.daysLeft + " days left)" %>
    </h3>

    <form id="commentForm" name="comment" method="post" action="/shelter/comment">
      Comment: <input type="text" name="comment" autofocus></input>
      <button id="btnSubmit" type="submit" value="<%= occupant.name %>">Submit</button>
    </form>

    <ul type="none">
      <% for (var comment in occupant.comments) %>
        <li> <%= occupant.comments %>
    </ul>

  </body>
</html>
